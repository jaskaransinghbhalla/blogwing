export type InputBoxType = {
  name: string;
  placeholder: string;
  type: string;
  onChange: any;
};

export type ButtonProps = {
  text: string;
  onClick: any;
};

export type SignupFormType = {
  username: string;
  email: string;
  password: string;
};
export type SignInFormType = {
  email: string;
  password: string;
};
